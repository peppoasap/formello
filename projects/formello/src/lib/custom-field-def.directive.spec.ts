import { CustomFieldDefDirective } from './custom-field-def.directive';

describe('CustomFieldDefDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFieldDefDirective();
    expect(directive).toBeTruthy();
  });
});
